{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "Vercel deploy for the API. Set Project Root Directory = apps/api in the dashboard.",
  "//install": "Install from the monorepo root so @parchhai/* workspace deps resolve, then generate Prisma.",
  "installCommand": "cd ../.. && corepack enable && pnpm install --no-frozen-lockfile && pnpm --filter @parchhai/db generate",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "includeFiles": "../../node_modules/.pnpm/@prisma+client*/**"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
